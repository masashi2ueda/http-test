version: '3'
services:
  pythontest:
    build:
      context: "./app"
      dockerfile: "Dockerfile"
      args:
        - UID=1000
        - GID=1000
        - USERNAME=user
        - GROUPNAME=user
    volumes:
      - ./app/src:/src
    tty: true
